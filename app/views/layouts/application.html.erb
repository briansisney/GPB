<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="row">
      <div>
        <% if current_user.nil? %>
          <%= link_to "Sign In", root_path, class: "large-3 columns button" %>
        <%else %>
          <%= link_to current_user.first_name, user_path(current_user.id), class: "large-3 columns button" %>
        <%end%> 
      </div>
      <div>
        <% if current_user.nil? %>
          <%= link_to "myBooks SignIn", my_books_path, class: "large-3 columns button" %>
        <%else %>
          <%= link_to "myBooks", user_path(current_user.id), class: "large-3 columns button" %>
        <%end%> 
      </div>
      <div>
        <%= link_to "How It Works", how_it_works_path, class: "large-3 columns button" %>
      </div>
      <div >
        <%= link_to "All Books", books_path, class: "large-3 columns button" %>
      </div>
    </div>
    
















    <%= yield %>
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>
    <footer class="row">
      <!-- <div class="large-10 columns">
      </div> -->
      <br/>
      <div class="large-4 columns">
        <% if current_user %>
          Signed in as <strong><%= "#{current_user.first_name} #{current_user.last_name}" %></strong>
          <br/>
          <%= link_to "Sign out", signout_path, id: "sign_out" %>
        <% else %>
          <%= link_to "", "/auth/facebook", id: "sign_in" %>
        <% end %>
      </div>
      <div class="large-4 columns">
      </div>
      <div class="large-4 columns righty">
        @2013 Brian, Greg, Stewart
      </div> 
    </footer>
    <%= javascript_include_tag "application" %>
  </body>
</html>